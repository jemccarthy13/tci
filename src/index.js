import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import TCI from './tci';

ReactDOM.render(
  <React.StrictMode>
    <TCI />
  </React.StrictMode>,
  document.getElementById('root')
);

